name: Build dist

on:
  push:
    branches: [main]
    paths:
      - src/**
      - scripts/**
      - package.json
      - package-lock.json
      - .github/workflows/build-dist.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Restore complete card source
        run: node scripts/restore-source.mjs

      - name: Install dependencies
        run: npm ci

      - name: Build HACS bundle
        run: npm run build

      - name: Check JavaScript syntax
        run: node --check dist/flight-orbit-3d-card.js

      - name: Commit restored source and dist
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/flight-orbit-3d-card.js dist/flight-orbit-3d-card.js
          if git diff --cached --quiet; then
            echo "source and dist are already current"
            exit 0
          fi
          git commit -m "chore: restore full card source and rebuild HACS bundle"
          git push
